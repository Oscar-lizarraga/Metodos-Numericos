/*Algoritmo de horner para eliminar el factor lineal de un polinomio*/

#include <stdio.h>

int main(int argc, char const *argv[])
{
	int i,n;
	float p,r;

	scanf("%d",&n);
	float a[n]; float b[n-1];

	for (int i = 0; i < n; i++)
	{
		scanf("%d",&a[i]);
		b[]
	}
	b[n-2] = a[n-1]				//recordemos que se resta 1 a la cantidad porque va de 0 a (n-1) elementos
	for (int i = 0; i < count; ++i)
	{
		/* code */
	}

	return 0;
}

